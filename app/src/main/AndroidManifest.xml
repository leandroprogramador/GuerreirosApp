<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leandro.guerreirosapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/LoginTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.CadastroActivity" />
        <activity android:name=".Activities.MainActivity" />
        <activity
            android:name=".Activities.GuerreirosActivity"
            android:label="@string/title_activity_guerreiros"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activities.NovoAlunoDados"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".Activities.NovoAlunoEndereco" />
        <activity android:name=".Activities.NovoAlunoModalidade"></activity>
    </application>

</manifest>